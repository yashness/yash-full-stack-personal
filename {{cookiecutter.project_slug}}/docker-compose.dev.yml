# =============================================================================
# Development Docker Compose
# =============================================================================
# Local development with hot reload and Traefik HTTPS routing.
#
# Usage:
#   docker compose -f docker-compose.dev.yml up
# =============================================================================

services:
  {%- if cookiecutter.use_frontend %}
  # ---------------------------------------------------------------------------
  # Frontend - Next.js + Bun
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: dev
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=https://api-{{ cookiecutter.project_slug }}.dev.test
      - BACKEND_INTERNAL_URL=http://backend:{{ cookiecutter.backend_port }}/api/v1
      # Clerk env vars (run `bun run setup:clerk` in frontend to enable auth)
      - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=${NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY:-pk_test_Y2VydGFpbi1qYWd1YXItMTkuY2xlcmsuYWNjb3VudHMuZGV2JA}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-sk_test_8gJB6j4KZAVoerpfdYF1qqiQOfUnNvJajMI3xw7ssA}
    networks:
      - traefik
      - internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.{{ cookiecutter.project_slug }}-frontend.rule=Host(`{{ cookiecutter.project_slug }}.dev.test`)"
      - "traefik.http.routers.{{ cookiecutter.project_slug }}-frontend.entrypoints=websecure"
      - "traefik.http.routers.{{ cookiecutter.project_slug }}-frontend.tls=true"
      - "traefik.http.services.{{ cookiecutter.project_slug }}-frontend.loadbalancer.server.port={{ cookiecutter.frontend_port }}"
    depends_on:
      - backend
    restart: unless-stopped
  {%- endif %}

  {%- if cookiecutter.use_backend %}
  # ---------------------------------------------------------------------------
  # Backend - FastAPI + uv
  # ---------------------------------------------------------------------------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: dev
    volumes:
      - ./backend:/app
      - backend-uv-cache:/root/.cache/uv
    environment:
      - ENVIRONMENT=development
      - FRONTEND_URL=https://{{ cookiecutter.project_slug }}.dev.test
      - DATABASE_URL=mysql://user:password@db:3306/{{ cookiecutter.project_slug | replace('-', '_') }}
      # Clerk env vars (run `bun run setup:clerk` in frontend to enable auth)
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY:-sk_test_8gJB6j4KZAVoerpfdYF1qqiQOfUnNvJajMI3xw7ssA}
      - CLERK_WEBHOOK_SECRET=${CLERK_WEBHOOK_SECRET:-whsec_RhtRvY27N5ZqIsm3pQ6cTJmbG6vADMVE}
    env_file:
      - ./backend/.env
    networks:
      - traefik
      - internal
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.{{ cookiecutter.project_slug }}-backend.rule=Host(`api-{{ cookiecutter.project_slug }}.dev.test`)"
      - "traefik.http.routers.{{ cookiecutter.project_slug }}-backend.entrypoints=websecure"
      - "traefik.http.routers.{{ cookiecutter.project_slug }}-backend.tls=true"
      - "traefik.http.services.{{ cookiecutter.project_slug }}-backend.loadbalancer.server.port={{ cookiecutter.backend_port }}"
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
  {%- endif %}

  # ---------------------------------------------------------------------------
  # MySQL Database
  # ---------------------------------------------------------------------------
  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: {{ cookiecutter.project_slug | replace('-', '_') }}
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - internal
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-prootpassword"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped

networks:
  traefik:
    external: true
  internal:
    driver: bridge

volumes:
  backend-uv-cache:
  mysql_data:
